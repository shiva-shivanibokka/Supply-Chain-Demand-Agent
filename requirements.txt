# requirements.txt — Hugging Face Spaces (Gradio SDK)
# Keep this file minimal. HF Spaces pre-installs gradio and its deps.
# Do NOT pin transitive deps — let pip use cached wheels from HF's layer.
# All packages here must have pre-built manylinux wheels (no source builds).

# Data
pandas
numpy

# Visualisation
plotly

# LLM providers
anthropic
openai

# Utilities
python-dotenv
