# requirements.txt — Hugging Face Spaces deployment
#
# All packages pinned to versions with pre-built cp311 manylinux wheels.
# No source builds — every package here downloads a .whl and installs instantly.
#
# gradio is intentionally omitted — HF Spaces installs it automatically.

pandas==2.2.2
numpy==1.26.4
anthropic==0.49.0
openai==1.55.3
plotly==5.24.1
python-dotenv==1.0.1
