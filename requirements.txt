# requirements.txt — Hugging Face Spaces deployment
#
# Pinned to exact versions with pre-built wheels only.
# gradio is omitted — HF Spaces installs it automatically.
# Packages are ordered lightest-first to reduce peak memory during resolution.

python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.2
plotly==5.24.1
httpx==0.27.2
anyio==4.4.0
anthropic==0.49.0
openai==1.55.3
