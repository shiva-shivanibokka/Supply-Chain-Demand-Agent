# requirements.txt — Hugging Face Spaces deployment
#
# Every package AND its transitive deps are pinned to versions with
# pre-built cp310 manylinux wheels. This prevents pip's resolver from
# downloading source tarballs or running Rust/C compiles, which causes
# OOMKilled on HF free tier.
#
# gradio is omitted — HF Spaces installs it automatically.

# ── Core data ──
numpy==1.26.4
pandas==2.2.2

# ── Visualisation ──
plotly==5.24.1

# ── Utilities ──
python-dotenv==1.0.1

# ── Shared transitive deps (pre-pinned to avoid resolver OOM) ──
httpx==0.27.2
anyio==4.4.0
sniffio==1.3.1
distro==1.9.0
certifi==2024.8.30
h11==0.14.0
idna==3.10
jiter==0.5.0

# ── LLM SDKs (lightest versions that still support tool_use) ──
anthropic==0.34.2
openai==1.40.0
