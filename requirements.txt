# requirements.txt — Hugging Face Spaces deployment
#
# Intentionally minimal. chromadb, torch, mlflow, sentence-transformers
# are excluded — they cause install timeouts on cloud free tier.
#
# The cloud app uses:
#   - rag/retriever_cloud.py  (numpy keyword search, no chromadb)
#   - rag/embeddings.npz      (pre-built embeddings, committed to repo)
#   - Statistical forecaster  (no torch/TFT needed)
#   - gradio_app.py           (Gradio UI, not Streamlit)
#
# For the full local stack (torch, chromadb, mlflow, streamlit):
#   pip install -r requirements-local.txt

pandas==2.1.4
numpy==1.26.4
anthropic==0.89.0
openai==2.30.0
plotly==6.6.0
gradio==5.34.2
python-dotenv==1.2.2
